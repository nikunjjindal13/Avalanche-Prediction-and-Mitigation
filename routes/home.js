const express=require('express')
const path =require('path')

const app = express.Router()


// app.get('/',(req,res,next)=>{
//     res.render('home')
// })
// app.post('/',(req,res,next)=>{
    // var globalVariable={
    //     lat: req.body.lat,
    //     long : req.body.long
    //  };

    //  res.redirect(`/api/data?lat=${globalVariable.lat}&lon=${globalVariable.lon}`);
    // const lat = req.body.lat;
    // const long = req.body.long;
    // res.redirect(`/api/data?lat=${lat}&lon=${lon}`);
    


// })

module.exports= app;